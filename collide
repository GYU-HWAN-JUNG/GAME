void collide(int i){  //충돌 및 사망
  dx[i] = x[i]-t;
  dy[i] = y[i]-s;
  d[i] = sqrt(dx[i]*dx[i]+dy[i]*dy[i]);
  if (d[i] <34){
    fill(102,0,23);
    textSize(70);
    text("You DIED...", 175, 150);
    delay(2000);
    stop();
  }
}
